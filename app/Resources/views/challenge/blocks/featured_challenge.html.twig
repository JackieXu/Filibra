<div class="medium-6 large-4 columns">
    <div class="challenge">
        <img class="challenge-thumbnail">
        <div class="challenge-details">
            <a href="{{ path('challenge_page', {'slug': challenge.slug}) }}">
                <h3 class="challenge-name">{{ challenge.name }}</h3>
            </a>
            <div class="challenge-detail detail row">
                <div class="detail-name medium-6 columns">Earnings pool</div>
                <div class="detail-name medium-6 columns text-right">{{ challenge.prize|currency }}</div>
            </div>
            <div class="challenge-detail detail row">
                <div class="detail-name medium-6 columns">Time left</div>
                <div class="detail-name medium-6 columns text-right">{{ challenge_datediff(challenge) }}</div>
            </div>
        </div>
    </div>
</div>
